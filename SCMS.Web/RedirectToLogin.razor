@inject NavigationManager Navigation

<section class="empty-state">
    <h1>Redirecting</h1>
</section>

@code {
    protected override void OnInitialized()
    {
        var target = Uri.EscapeDataString(Navigation.ToBaseRelativePath(Navigation.Uri));
        Navigation.NavigateTo($"/login?returnUrl={target}");
    }
}
